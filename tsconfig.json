{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "baseUrl": ".", // 프로젝트 루트를 기준으로
    "paths": {
      "@/*": ["src/*"],           // 예: @/api/axios → src/api/axios.ts
      "@shared/*": ["shared/*"]   // 예: @shared/types/user.ts
    }
  }
}
